//sets do not guarantee order
//can be kept sorted
//won't allow duplicates
//maintain uniqueness
//is not a subtype of Seq
//has mutable & immutable implementations

val set = Set(1, 2, 3, 5, 4, 9, 4)

//order is determined with the hashing algorithm
//if want to send to Seq func, would need transform 1st
